# md-blueprints-environment-model: v1
name: Cleanup Preview Blueprints

permissions:
  contents: read

on:
  pull_request:
    types:
      - closed
  delete:

jobs:
  cleanup:
    uses: ./.github/workflows/reusable_cleanup_preview_blueprints.yaml
    secrets: inherit
